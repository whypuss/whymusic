<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--
      啟動全程保持黑底。App 介面是深色的，預設的白色 splash／白色視窗背景會讓
      冷啟動先白屏好幾秒再跳成黑 —— 使用者看到的就是「一直在載入」。
      三層都要蓋到：舊系統的 splash drawable、Android 12+ 的系統 splash 底色、
      以及 WebView 首繪前的視窗背景（WebView 本身的底色由 capacitor.config.json
      的 backgroundColor 管）。
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">#FF000000</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="windowSplashScreenBackground">#FF000000</item>
    </style>
</resources>